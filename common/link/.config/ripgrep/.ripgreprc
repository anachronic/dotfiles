# Don't exclude hidden paths
--hidden

# Exclude lockfiles
--glob
!{package-lock.json,yarn.lock,poetry.lock,Gemfile.lock,pnpm-lock.yaml}

# Exclude annoying pycache folders
--glob
!__pycache__/*

# Exclude more annoying python files
--glob
!*.pyc

# Not really interested in svgs
--glob
!*.svg

# Exclude git folder
--glob
!.git

# Exclude node_modules because we'd go crazy otherwise
--glob
!node_modules

# Exclude everything from dotfiles submodules
--glob
!common/link/.config/nvim/pack/bundle/opt/**

# vim: ft=conf
